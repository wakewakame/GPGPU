struct input_data
{
	float arr[loop];
};

struct output_data
{
	float arr[loop];
};

main()
{
	for (int i = 0; i < loop; i++)
	{
		output_data.arr[index] += sin(
			input_data.arr[i]
		);
		output_data.arr[index] /= loop;
	}	
}